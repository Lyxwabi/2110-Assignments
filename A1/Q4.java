import java.util.Collections;
import java.util.Scanner;
import java.util.PriorityQueue;

public class Q4 {
  public static void main(String[] args) {
    /* The process of getting integers from input is the same with
     * other questions
     */

    // Create a maxPriorityQueue
    PriorityQueue<Integer> youDecideTheName = new PriorityQueue<>(Collections.reverseOrder());

    // Use for add() to add elements to the PriorityQueue in the 
    // for loop

    // Print k times
    // for 0 : k { print(youDecideTheName.poll()) }
  }
}
